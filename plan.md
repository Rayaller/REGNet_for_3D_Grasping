# REGNet_for_3D_Grasping v2 发布与服务器训练改造计划

## 当前进展（2026-04-07）

- 阶段 1 已有明显落地：
  - 发布边界已整理到 `release_scope.md`
  - `.gitignore` 已开始按源码 / 数据 / 产物边界调整
- 阶段 2 和阶段 3 正在收尾：
  - `requirements.txt` 已改成标准 `pip` 约束格式
  - 已新增 `scripts/install_extensions.sh` 与 `scripts/check_runtime.py`
  - `train.py`、`test.py`、`utils.py` 等入口已开始去硬编码路径、增强默认参数与日志输出
  - CUDA 扩展编译链路已补上离线环境与无可见 GPU 编译环境的兼容处理
- 阶段 4 已完成一轮最小验证：
  - 现已补齐 GPU 环境下的 `validate_region`、完整 `validate` 与 `test.py` 端到端实跑
  - 关键 Python 入口通过语法检查
  - `pn2_ext` 与 `dgcnn_ext` 可在源码树内编译成功
  - `scripts/check_runtime.py` 运行通过
  - `validate_score` 已基于 `/tmp/regnet_tiny_dataset` 完成一次 smoke run
- 阶段 5 已完成：
  - `README.md` 已重写为服务器部署与复现实操手册
- 阶段 6 已完成：
  - 本地已创建 `v2` 分支
  - 发布提交为 `ca5fb23` (`Prepare server-ready v2 release`)
  - 远端 `origin/v2` 已推送成功
- 仍未完全闭环的部分：
  - `test.py` 对不同样例的 `table_height` 仍需在文档里明确说明，否则默认值可能把候选抓取全部过滤掉
- 当前最合适的下一步：
  - 如需要，可继续开 PR 或补一份更简短的 release note
  - 为发布版补齐“外部数据如何准备”的文档说明

## 目标

将当前 [REGNet_for_3D_Grasping](/home/ray/work/regnet/REGNet_for_3D_Grasping) 整理为一个可以推送到 GitHub 的 `v2` 版本，使服务器能够：

1. 从 GitHub 正常克隆项目
2. 按 README 提供的步骤完成环境配置和依赖安装
3. 正常编译 PointNet++ / DGCNN 相关 CUDA 扩展
4. 正常启动训练，不因路径、依赖、扩展或默认参数问题中断
5. 按文档完成预训练、联合训练、验证、测试和可视化

## 当前已知问题

- README 仍是原始仓库风格，缺少“继承自之前工作”的说明，也不适合直接指导服务器部署
- `requirements.txt` 过旧且不是标准 `pip` 约束格式，服务器安装时容易出错
- `pn2_utils` 与 `functions` 依赖 CUDA 扩展，当前仓库缺少面向新环境的明确编译与验证流程
- 训练与测试流程里仍存在服务器环境敏感点：
  - CUDA 初始化
  - DataLoader worker 行为
  - 本地路径假设
  - 扩展导入依赖
- 仓库中同时存在数据、预测结果、模型和日志，不适合直接作为可下载训练仓库发布

## 总体策略

采用“先整理可发布仓库，再做服务器训练验证，最后发布到 GitHub `v2` 分支”的方式推进。

不直接把当前工作区原样推上去，而是先完成：

- 代码可复现
- 安装可复现
- 训练命令可复现
- 文档可复现

之后再发布。

## 分阶段计划

### 阶段 1：明确 v2 发布边界

#### 要做什么

- 明确 `v2` 仓库中应保留的内容：
  - 源码
  - 文档
  - 扩展源码
  - 必要脚本
  - 示例目录结构
- 明确应排除的内容：
  - `dataset`
  - `eval_data`
  - `test_file` 中的预测产物
  - `assets/models`
  - `assets/log`
  - 构建产物、缓存、临时输出
- 整理 `.gitignore`

#### 目标效果

- 仓库体积合理
- 新服务器 `git clone` 后不会拉到无关大文件
- 仓库内容和训练用途一致

#### 验收标准

- `git status` 中仅包含应发布的源码与文档改动
- 仓库内不再混入数据、模型、日志和预测结果

### 阶段 2：梳理并加固依赖安装流程

#### 要做什么

- 审查现有 `requirements.txt`
- 确定服务器训练所需的最小依赖集合：
  - Python
  - PyTorch
  - torchvision
  - Open3D
  - tensorboardX
  - transforms3d
  - 其他运行时依赖
- 明确 CUDA / PyTorch 版本兼容关系
- 为 `pn2_utils` 和 `pn2_utils/functions` 提供稳定的编译安装方式
- 如有必要，补充安装脚本或更清晰的命令顺序

#### 目标效果

- 一台干净服务器能够按固定流程装好环境
- 编译扩展时不需要依赖“手工猜路径”
- 依赖失败时有明确排查入口

#### 验收标准

- 在新环境中能够完成：
  - Python 环境创建
  - pip/conda 依赖安装
  - CUDA 扩展编译
  - Python 导入核心模块成功

### 阶段 3：清理服务器训练阻塞点

#### 要做什么

- 排查训练入口和测试入口中的环境耦合问题
- 去掉或修正不适合服务器场景的默认假设，例如：
  - 硬编码路径
  - GPU 初始化假设
  - 仅在当前机器可用的依赖路径
  - worker / CPU / CUDA 分支不一致
- 为训练命令补齐更稳定的 CLI 参数和默认行为
- 确保 fresh clone 后，只通过 README 中的命令就能启动训练

#### 目标效果

- 训练命令在服务器上可直接运行
- 没有“必须先手工改源码里的路径/参数”这类隐含步骤

#### 验收标准

- 从命令行直接执行预训练或训练命令时，不会因路径或初始化问题立即失败
- 基础 smoke run 可以正常启动

### 阶段 4：做服务器可复现验证

#### 要做什么

- 在干净环境里按 README 的顺序执行一轮最小可复现流程
- 验证内容至少包括：
  - 环境安装
  - 扩展编译
  - 关键模块导入
  - 最小训练命令启动
  - 最小验证 / 测试命令启动
- 记录所有关键命令、报错、修复和最终结果

#### 目标效果

- README 不是“理论上可行”，而是“按文档实测可行”

#### 验收标准

- 至少完成一轮从环境安装到训练启动的实跑验证
- 所有命令都能在新环境中复现

### 阶段 5：重写 README

#### 要做什么

- 重写 README，使其面向当前项目维护版本，而不是原始论文仓库说明
- README 必须包含以下内容：
  - 项目说明：本项目继承自之前的工作，并说明继承来源
  - 项目结构说明
  - 服务器环境准备
  - 依赖安装
  - CUDA 扩展编译
  - 数据准备要求
  - 训练命令
  - 验证命令
  - 测试命令
  - 可视化命令
  - 常见问题排查
- README 中所有命令都使用当前仓库真实可执行的版本

#### 目标效果

- 服务器用户只看 README 就能完成整个训练流程
- 不需要额外口头补充“还有几处要手改”

#### 验收标准

- README 中的命令已被实际验证
- README 明确写出依赖和运行顺序

### 阶段 6：发布 GitHub v2

#### 要做什么

- 整理最终发布内容
- 创建 `v2` 分支
- 提交服务器可训练版本
- 推送到之前的 GitHub 仓库
- 验证远端仓库可被服务器正常克隆

#### 目标效果

- `origin/v2` 成为一个可以直接拉取训练的版本

#### 验收标准

- `git clone -b v2 ...` 可成功
- clone 后按 README 能完成环境安装和训练启动

## 预计执行顺序

1. 整理发布边界和 `.gitignore`
2. 加固依赖与扩展安装
3. 清理训练/测试入口中的服务器阻塞点
4. 做最小到完整的服务器验证
5. 重写 README
6. 发布到 GitHub `v2`

## 过程中需要特别注意的点

- 不把数据和训练产物提交到 `v2`
- 不依赖当前本机的隐式环境变量才能运行
- 不把“手工修改源码路径”作为 README 的必要步骤
- 每个关键步骤都要留日志，便于回溯
- 在推送 `v2` 前，确保工作区只包含应该发布的内容

## 最终交付物

完成后应得到：

- 一个可发布的 [REGNet_for_3D_Grasping](/home/ray/work/regnet/REGNet_for_3D_Grasping) `v2` 版本
- 一份可直接指导服务器完成训练的 README
- 一套经过验证的环境安装与训练命令
- 一个已推送到 GitHub 的 `v2` 分支
